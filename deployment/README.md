# Deployment Configurations
## Secure Real-Time Remote Code Execution Laboratory Platform

This directory contains production deployment infrastructure specifications:
- `docker-compose.prod.yml`: Multi-service production stack.
- `nginx.conf`: Nginx reverse proxy configuration for SSL termination, static file serving, and WebSocket upgrading.
